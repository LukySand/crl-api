node_modules/
prisma/generated
dist
*.log

# env (nunca commitear secretos) — cubre .env, .env.local, backups, etc.
.env*
!.env.example

# helper local para poblar datos de demo (tiene passwords de prueba)
scripts/seed-demo-users.ts

# tooling local (skill registry) — no va al repo
.atl/

# archivos subidos (fotos de perfil, espacios): default de Storage sin FILES_STORAGE_PATH
uploads/
